dist: xenial   # required for Python >= 3.7
language: python
python:
  - "3.7"
  # PyPy versions
  - "pypy3.5"
# command to install dependencies
install:
  - pip install .
# command to run tests
script: pytest
